# Руководство по запуску и развёртыванию сайта Мега-Сервис-Групп

## 📋 Содержание

1. [Запуск локально](#запуск-локально)
2. [Структура проекта](#структура-проекта)
3. [Привязка своего домена](#привязка-своего-домена)
4. [Развёртывание на сервер](#развёртывание-на-сервер)
5. [Управление контентом](#управление-контентом)

---

## 🚀 Запуск локально

### Требования

Убедитесь, что на вашем компьютере установлены:

- **Node.js** версии 18+ ([скачать](https://nodejs.org/))
- **npm** или **pnpm** (идёт в комплекте с Node.js)
- **Git** для управления версиями

### Шаг 1: Скачайте проект

```bash
# Скачайте все файлы проекта через Management UI
# Или используйте Git (если у вас есть репозиторий)
git clone <ваш-репозиторий> mega-service-group-landing
cd mega-service-group-landing
```

### Шаг 2: Установите зависимости

```bash
# Используя pnpm (рекомендуется)
pnpm install

# Или используя npm
npm install
```

### Шаг 3: Создайте файл .env.local

Создайте файл `.env.local` в корне проекта с переменными окружения:

```env
# Базовые переменные (обычно уже установлены)
DATABASE_URL=mysql://user:password@localhost:3306/mega_service
JWT_SECRET=your-secret-key-here
VITE_APP_ID=your-app-id
OAUTH_SERVER_URL=https://api.manus.im
VITE_OAUTH_PORTAL_URL=https://oauth.manus.im
OWNER_OPEN_ID=your-owner-id
OWNER_NAME=Ваше имя
VITE_APP_TITLE=Мега-Сервис-Групп
VITE_APP_LOGO=https://example.com/logo.png
```

### Шаг 4: Запустите dev сервер

```bash
# Используя pnpm
pnpm dev

# Или используя npm
npm run dev
```

Сайт будет доступен по адресу: **http://localhost:5173**

### Шаг 5: Откройте в браузере

Перейдите по адресу http://localhost:5173 и вы увидите ваш сайт!

---

## 📁 Структура проекта

```
mega-service-group-landing/
├── client/                          # Frontend (React)
│   ├── src/
│   │   ├── pages/
│   │   │   ├── Home.tsx            # Главная страница с лендингом
│   │   │   ├── AdminPanel.tsx      # Админ-панель для управления заявками
│   │   │   ├── AdminLogin.tsx      # Страница входа в админ-панель
│   │   │   └── NotFound.tsx        # Страница 404
│   │   ├── components/             # Переиспользуемые компоненты
│   │   ├── lib/
│   │   │   └── trpc.ts             # tRPC клиент
│   │   ├── App.tsx                 # Маршруты приложения
│   │   ├── main.tsx                # Точка входа
│   │   └── index.css               # Глобальные стили
│   ├── public/                     # Статические файлы (логотип, изображения)
│   └── index.html
├── server/                          # Backend (Express + tRPC)
│   ├── routers.ts                  # API процедуры
│   ├── db.ts                       # Функции работы с БД
│   └── _core/                      # Внутренние утилиты
├── drizzle/
│   ├── schema.ts                   # Схема базы данных
│   └── migrations/                 # Миграции БД
├── package.json                    # Зависимости проекта
├── tsconfig.json                   # Конфигурация TypeScript
├── vite.config.ts                  # Конфигурация Vite
└── drizzle.config.ts               # Конфигурация Drizzle ORM
```

---

## 🌐 Привязка своего домена

### Вариант 1: Через Manus (Рекомендуется)

Если вы развёртываете через платформу Manus:

1. **Откройте Management UI** (кнопка в правом углу сайта)
2. **Перейдите в Settings → Domains**
3. **Нажмите "Add Custom Domain"**
4. **Введите ваш домен** (например, `mega-service.ru`)
5. **Скопируйте DNS записи** которые вам дадут
6. **Добавьте эти записи в панель управления вашего хостинга**

Обычно нужно добавить:
- **CNAME запись**: `mega-service.ru` → `xxx.manus.space`
- Или **A запись** с IP адресом Manus

### Вариант 2: Через собственный хостинг

Если вы развёртываете на своем сервере:

#### 2.1 Подготовка сервера

```bash
# Установите Node.js и npm на сервер
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Установите PM2 для управления процессом
sudo npm install -g pm2
```

#### 2.2 Загрузите проект на сервер

```bash
# На вашем компьютере
scp -r mega-service-group-landing user@your-server.com:/home/user/

# Или используйте Git
ssh user@your-server.com
cd /home/user
git clone <ваш-репозиторий> mega-service-group-landing
```

#### 2.3 Установите зависимости на сервере

```bash
cd /home/user/mega-service-group-landing
npm install
```

#### 2.4 Создайте .env файл на сервере

```bash
nano .env.production
```

Добавьте переменные окружения (см. выше)

#### 2.5 Соберите проект

```bash
npm run build
```

#### 2.6 Запустите с PM2

```bash
# Запустите приложение
pm2 start npm --name "mega-service" -- start

# Сохраните конфигурацию PM2
pm2 save

# Настройте автозапуск при перезагрузке
pm2 startup
```

#### 2.7 Настройте Nginx как reverse proxy

```bash
# Создайте конфиг Nginx
sudo nano /etc/nginx/sites-available/mega-service

# Добавьте конфиг:
server {
    listen 80;
    server_name mega-service.ru www.mega-service.ru;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# Активируйте сайт
sudo ln -s /etc/nginx/sites-available/mega-service /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

#### 2.8 Добавьте SSL сертификат (Let's Encrypt)

```bash
# Установите Certbot
sudo apt-get install certbot python3-certbot-nginx

# Получите сертификат
sudo certbot --nginx -d mega-service.ru -d www.mega-service.ru
```

### Вариант 3: Через Cloudflare

Если вы используете Cloudflare для управления DNS:

1. **Добавьте домен в Cloudflare**
2. **Обновите NS записи** у вашего регистратора домена
3. **В Cloudflare создайте CNAME запись**:
   - Name: `@` (или имя поддомена)
   - Content: `xxx.manus.space` (или IP вашего сервера)
   - Proxy status: Proxied (оранжевое облако)
4. **Включите SSL** в Cloudflare (Settings → SSL/TLS → Full)

---

## 🔧 Управление контентом

### Изменение текста на сайте

Все тексты находятся в файле `client/src/pages/Home.tsx`:

```typescript
// Измените заголовок
<h1>Новый заголовок</h1>

// Измените описание
<p>Новое описание</p>
```

### Добавление новых изображений

1. **Поместите изображение** в папку `client/public/`
2. **Используйте в коде**:
```tsx
<img src="/image-name.jpg" alt="Описание" />
```

### Изменение цветов

Все цвета определены в файле `client/src/index.css`:

```css
:root {
  --primary: #0f5a6b;        /* Темно-синий */
  --background: #f5f3f0;     /* Бежевый фон */
  --foreground: #1a1a1a;     /* Чёрный текст */
}
```

### Добавление новых услуг

В файле `client/src/pages/Home.tsx` найдите массив `services` и добавьте новую услугу:

```typescript
const services = [
  // ... существующие услуги
  { 
    title: 'Новая услуга', 
    description: 'Описание услуги', 
    icon: 'blueprint' // или другой иконка
  },
];
```

---

## 📊 Управление заявками

### Доступ к админ-панели

1. **Перейдите на** `/admin-login`
2. **Логин**: `admin`
3. **Пароль**: `megapro11`
4. **Нажмите "Войти"**

### В админ-панели вы можете:

- **Просмотреть все заявки** с полной информацией
- **Отсортировать** по дате или имени
- **Удалить** ненужные заявки
- **Позвонить или написать** клиенту (прямые ссылки)
- **Просмотреть статистику** (всего заявок, за 7 дней, за 24 часа)

### Где хранятся заявки

Все заявки сохраняются в базе данных в таблице `formSubmissions`. Вы можете:

- Просмотреть их в **Management UI → Database → formSubmissions**
- Экспортировать в CSV через админ-панель (если добавите эту функцию)

---

## 🚀 Развёртывание обновлений

### После изменений на локальной машине

```bash
# 1. Протестируйте локально
pnpm dev

# 2. Создайте checkpoint (если используете Manus)
# Через Management UI → Publish

# 3. Или загрузите на сервер
git push origin main
ssh user@your-server.com
cd /home/user/mega-service-group-landing
git pull origin main
npm run build
pm2 restart mega-service
```

---

## 🐛 Решение проблем

### Сайт не загружается

```bash
# Проверьте, запущен ли dev сервер
pnpm dev

# Проверьте консоль браузера на ошибки (F12)
# Проверьте терминал на ошибки сервера
```

### Форма не отправляется

```bash
# Проверьте, что сервер запущен
# Проверьте переменные окружения в .env
# Проверьте консоль браузера на ошибки CORS
```

### Админ-панель не открывается

```bash
# Убедитесь, что вы вошли с правильными данными
# Логин: admin
# Пароль: megapro11

# Очистите cookies браузера и попробуйте снова
```

### БД не подключается

```bash
# Проверьте DATABASE_URL в .env
# Убедитесь, что БД сервер запущен
# Проверьте права доступа к БД
```

---

## 📞 Поддержка

Если у вас возникли вопросы:

1. **Проверьте логи** в терминале
2. **Прочитайте документацию** проекта
3. **Свяжитесь с разработчиком** через Manus

---

## 🎉 Готово!

Ваш сайт полностью настроен и готов к работе. Удачи в развитии вашего бизнеса!
